// var commentSlick = new Slick(document.querySelector('.main-slider'), {infinite: true, dots: true});

// commentSlick.unslick();
$('.main-slider').slick({
    "slidesToShow": 2,
    "slidesToScroll": 2
});
